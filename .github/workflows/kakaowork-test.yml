name: Kakaowork Lunch Vote (Daily 23:05)

on:
  schedule:
    # 매일 오후 11:05 KST = 14:05 UTC
    - cron: "5 14 * * *"

  # 수동 실행도 가능
  workflow_dispatch:

jobs:
  send-message:
    runs-on: ubuntu-latest

    steps:
      - name: Log trigger time
        run: |
          echo "🚀 Workflow triggered"
          echo "UTC time: $(date -u)"
          echo "KST time: $(TZ=Asia/Seoul date)"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pytz

      - name: Send KakaoWork message
        env:
          PRACTICE_CODE: ${{ secrets.PRACTICE_CODE }}
        run: |
          python - << 'EOF'
          import os
          import requests

          url = os.getenv("PRACTICE_CODE")
          if not url:
              raise Exception("PRACTICE_CODE is not set")

          payload = {
              "text": "🍚 밥밥\n함점은 👏 \n따점은 👍 \n눌러주세요"
          }

          res = requests.post(url, json=payload, timeout=15)
          print("status:", res.status_code)
          print("response:", res.text)

          if res.status_code not in (200, 204):
              raise Exception("Failed to send KakaoWork message")

          print("✅ Message sent.")
          EOF
