name: Kakaowork Lunch Vote

on:
  # GitHub Actions cron은 기본적으로 UTC 기준
  # 오전 9:30 KST = 00:30 UTC
  # 월~금(1-5)만 실행
  schedule:
    - cron: "30 0 * * 1-5"

  # 필요하면 수동 실행도 가능
  workflow_dispatch:

jobs:
  send-message:
    runs-on: ubuntu-latest

    steps:
      - name: Log trigger time
        run: |
          echo "🚀 Workflow triggered"
          echo "UTC time: $(date -u)"
          echo "KST time: $(TZ=Asia/Seoul date)"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pytz

      - name: Send KakaoWork message
        env:
          PRACTICE_CODE: ${{ secrets.PRACTICE_CODE }}
        run: |
          python - << 'EOF'
          import os
          import sys
          import requests
          from datetime import datetime
          import pytz

          webhook_url = os.getenv("PRACTICE_CODE")
          if not webhook_url:
              raise Exception("PRACTICE_CODE is not set. GitHub Secrets에 PRACTICE_CODE를 추가했는지 확인하세요.")

          kst = pytz.timezone("Asia/Seoul")
          now = datetime.now(kst)

          # 안전장치: 주말이면 절대 발송 안 함
          if now.weekday() >= 5:  # 5=토, 6=일
              print("🚫 Weekend detected (KST). Skipping send.")
              sys.exit(0)

          payload = {
              "text": "🍚 밥밥\n함점은 👏 \n따점은 👍 \n눌러주세요"
          }

          res = requests.post(webhook_url, json=payload, timeout=15)
          print("status:", res.status_code)
          print("response:", res.text)

          if res.status_code not in (200, 204):
              raise Exception("Failed to send KakaoWork message")

          print("✅ Message sent.")
          EOF
